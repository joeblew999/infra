# test taskfile

# https://taskfile.dev

# https://taskfile.dev/reference/cli

# CI calls this for smoke tests and debugging..


version: '3'

includes:
  task:
    taskfile: './../taskfiles/task_taskfile.yml'
  dummy:
    taskfile: './../taskfiles/dummy_taskfile.yml'
  
vars:
  

tasks:
  default:
    desc: List operations and state.
    cmds:
      - echo
      - echo "Runs Incus on a macOS host using Macpine."
      - echo
      - task --list-all
      - echo
    silent: true

  this:dummy:run:
    desc: Run the dummy taskfile
    cmds:
      - echo "Running the dummy taskfile..."
      - task dummy:dep

  

  

  


  

      
